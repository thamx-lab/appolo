# 🐑 Black Sheep Sanctuary — Project History & Handover

This file is a saved summary of our entire conversation and development process. It serves as a record of everything we built and configured for your gym website.

## 1. Project Rebranding
* **Previous Name:** Rolex Gym
* **New Name:** Black Sheep Sanctuary
* **Theme:** Premium, dark aesthetic with neon-red accents (`#ff2e2e`).
* **Contact Details Updated:** 
  * Phone: `+91 9345812081`
  * Email: `k26647300@gmail.com`

## 2. New Features Built
* **User Dashboard (`UserDashboard.jsx`):** 
  * Created a personal command center for users.
  * Added widgets for: Hydration tracking, Calorie tracking, Workout Reminders, and a BMI History chart.
  * *Note:* All dashboard data is currently saved locally on the user's device (`localStorage`).
* **Dynamic Pricing:** 
  * Plans automatically adjust depending on currency (Silver ₹600, Gold ₹800, Platinum ₹1000).

## 3. Security Hardening (Backend)
* Implemented **CORS** to block unauthorized websites from calling your API.
* Added **Helmet** to protect against Cross-Site Scripting (XSS) and Clickjacking.
* Added **Express Rate Limiter** (max 40 requests per minute) to prevent DDoS attacks and spam.
* Added strict **Email Validation** for all API endpoints.

## 4. Payment Gateway Integration (Razorpay)
We completely upgraded the payment system from a dummy simulator to a real, secure Razorpay flow.
* **Server-Side Order Creation:** The backend sets the price (₹600, ₹800, ₹1000) so hackers cannot change the price in the browser.
* **Signature Verification:** The backend uses `HMAC-SHA256` encryption to verify the `razorpay_signature` before recording a payment.
* **FamPay Support:** Verified that teenagers can use their FamPay UPI ID or FamPay Card on the checkout screen.
* **Fallback Protection:** Fixed a bug where missing API keys would crash the server. Now, the server stays alive even if keys are missing.

## 5. Database Setup (Supabase)
* Created tables for `plans`, `members`, `contacts`, and `payments`.
* Upgraded the `payments` table to include the Razorpay `order_id` for perfect tracking.

## 6. How to Go Live (Final Checklist)
1. Get your **Key ID** and **Key Secret** from [Razorpay](https://razorpay.com).
2. Get your **Supabase URL** and **Anon Key** from [Supabase](https://supabase.com).
3. Put all 4 keys into your **Render Dashboard -> Environment Variables**.
4. Run the `schema.sql` code inside the Supabase SQL Editor.

## 7. Useful Commands
* **Run Frontend locally:** `cd frontend` then `npm run dev`
* **Run Backend locally:** `cd backend` then `node api/index.js`
* **Build Frontend:** `npm run build`

---
*Generated by Antigravity AI Assistant.*
